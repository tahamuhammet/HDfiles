% create vector and matrices
a=[1 2];
b=[3 4];
c=[a;b]

% arithmetic operations
e=7;
f=8;
g=e*f;

%answer for the last calculation
3^4;
2^2;


%plotting
x=linspace(0,4*pi,1000);
y=sin(x)
plot(y);
plot(x,y);

% to convert selected lines into comment, use CTRL+R, to undo it use CTRL+T